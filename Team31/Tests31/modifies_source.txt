procedure Modifies1 {
    a = b + b1 / (b2 * (b3 % b4) - b5);
    print c;
    read c;
    if (d == e) then {
        read Modified4;
        while (g != h) {
            read Modified5; 
            if (c1 == c2) then {
                print notModified;} else {
                read Modified1;}} } else {
        print j;
        while (k >= l) {
            read Modified2; }}
    while (C1 == c2) {
        print notModified;
        if (c1 == c2) then {
            print notModified; 
            while (c1 == c2) {
                read Modified3; }} else {
            print NotModified; }}
    while ((!(n <= o)) || ((p > q) && (r < s))) {
        print notModified; }
    if (c1 == c2) then {
        print notModified; } else {
        print notModified; } 
    call Modifies2; 
    call Modifies3; }
procedure Modifies2 {
    read Modified6; 
    call Modifies4; }
procedure Modifies3 {
    print u; }
procedure Modifies4 {
    if (c1 == c2) then {
        read Modified7; } else {
        print w; }}
